/**
 * Engineering constants for wire processing and reel calculations.
 */

/**
 * Reel Factor Constant (Imperial)
 * Used in the formula: F = (H + D_barrel) * H * Traverse * 0.262
 * This constant converts cubic inches to feet for wire reel capacity estimations.
 */
export const REEL_FACTOR_CONSTANT = 0.262;

export const METERS_TO_FEET = 3.280839895;
export const INCHES_TO_METERS = 0.0254;
